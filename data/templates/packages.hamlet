<div #content>
    <h1 .pageTitle>
        <a href=@{Base}/#{pageUrl}> #{pagetitle}
    <table>
        <tr>
            <td>Program
            <td>Module
            <td>Default version
            <td>Description
        $forall it <- packages
            <tr>
                <td>
                    <a href=@{Base}/#{it infoUrl}>#{it displayName}
                <td>
                    <a @{Base}/#{(it defaultVersion) helpPageHref}>#{it moduleName}
                <td>
                    <a href=@{Base}/#{it versionPageUrl}>#{it defaultVersionName}
                <td>#{it.description}
